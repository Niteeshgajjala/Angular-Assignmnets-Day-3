import { LowGradeDirective } from './low-grade.directive';

describe('LowGradeDirective', () => {
  it('should create an instance', () => {
    const directive = new LowGradeDirective();
    expect(directive).toBeTruthy();
  });
});
